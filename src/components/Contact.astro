---
const pathname = Astro.url.pathname;
---

<div class="region">
  <div class="box flow prose bg-gray-200" id="contact">
    <h2 class="mt-zero">Request for comment</h2>
    <p>
      Have a question or a comment about this topic, I'd love to discuss it
      more! Fill this form out!
    </p>
    <form
      name="main"
      netlify-honeypot="bot-field"
      class="switcher"
      method="POST"
      data-netlify="true"
      action="/thank-you/"
    >
      <input
        type="hidden"
        name="subject"
        value={`🤖 Form submission: frankstallone.net${pathname}`}
      />
      <p class="hidden">
        <label for="bot-field">Do you like delicious treats?</label>
        <input
          name="bot-field"
          id="bot-field"
          class="border-gray-700 border-1 border-solid"
        />
      </p>
      <span class="stack">
        <label for="name">Name</label>
        <input
          id="name"
          name="name"
          type="text"
          required
          class="border-gray-700 border-1 border-solid"
        />
      </span>
      <span class="stack">
        <label for="email">Email</label>
        <input
          id="email"
          name="email"
          type="email"
          required
          class="border-gray-700 border-1 border-solid"
        />
      </span>
      <span class="stack">
        <label for="message">Message</label>
        <textarea
          name="message"
          id="message"
          rows="6"
          required
          class="border-gray-700 border-1 border-solid"></textarea>
      </span>
      <button type="submit" class="button">Submit</button>
    </form>
  </div>
</div>
